
AST erstellen (left right)

(redirect dann vielleicht) pipe dann  commands dann word in der order (hirarchie)
pipe zeigt auf command links und command rechts, command zeigt auf word


		   pipe
		/		\
	command		command
	/		/	  /	   \
word	word	word	word

toms github -> inspo

function die nodes abspielen kann

pipe - pipe_execution(pipe zeug (pipe command))
	in pipe execution die grose execution function (eveluate_ast) aufrufen
		linkd -- if type_command execute command
		rechts -- if type_command execute command



